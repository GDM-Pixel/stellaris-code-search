node_modules/
dist/
.env
.env.local
.vectors/
*.log
*.tsbuildinfo
# Local config — environment-specific, not for sharing
.stellarisrc
# ...but keep test fixture rc files (needed by the resolver test suite)
!test/fixtures/**/.stellarisrc
.claude/
# Internal audit file — not for public repo
AUDIT.md
# Internal dev instructions — exposes architecture details, not for public repo
CLAUDE.md
